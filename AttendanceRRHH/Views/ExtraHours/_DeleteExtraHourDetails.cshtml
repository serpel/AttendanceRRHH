@model AttendanceRRHH.Models.ExtraHourDetail
@using Resources

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
    <h4 class="modal-title" id="myModalLabel">@Resources.Delete</h4>
</div>

@using (Html.BeginForm("DeleteExtraHourDetails", "ExtraHours", FormMethod.Post))
{
    <div class="modal-body">

        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ExtraHourDetailId)
        @Html.HiddenFor(model => model.ExtraHourId)

        <div class="row form-row">
            <div class="col-md-2">
                <label class="form-label">@Resources.Day</label>
                @Html.EditorFor(model => model.Day, new { htmlAttributes = new { @class = "form-control", @readonly = "readonly" } })
            </div>
            <div class="col-md-2">
                <label class="form-label">@Resources.Code</label>
                @Html.EditorFor(model => model.Code, new { htmlAttributes = new { @class = "form-control", @readonly = "readonly" } })
            </div>
            <div class="col-md-4">
                <label class="form-label">@Resources.StartHour</label>
                @Html.EditorFor(model => model.StartHour, new { htmlAttributes = new { @class = "form-control", @readonly = "readonly" } })
            </div>
            <div class="col-md-4">
                <label class="form-label">@Resources.EndHour</label>
                @Html.EditorFor(model => model.EndHour, new { htmlAttributes = new { @class = "form-control", @readonly = "readonly" } })
            </div>
        </div>
    </div>
    <div class="modal-footer">
        <button class="btn" type="button" data-dismiss="modal">@Resources.Cancel</button>
        <input class="btn btn-primary" type="submit" value="@Resources.Save" />
    </div>
}